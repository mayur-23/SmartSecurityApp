<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.smartsecurity">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartSecurity">
        <activity android:name=".form"></activity>
        <activity
            android:name=".student_qr_generator"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".login_student"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".login_type_select"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".first_page"
            android:theme="@style/Theme.Design.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ScannerView"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".Qrscanner"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".Login"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".Register"
            android:theme="@style/Theme.Design.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.Design.NoActionBar" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>